{
  "format_version": "1.8.0",
  "render_controllers": {
    "controller.render.player.cape_custom": {
      // because emotes have hardcoded animations so we need to parent to the body bone to inherit animations
      "geometry": "query.is_emoting ? geometry.cape_emote_fix : geometry.cape",
      "materials": [ { "*": "material.default" } ],
      "textures": [ "texture.cape" ],
      "part_visibility": [
        { "waist": 0 } // to be consistent with vanilla because idk what it does
      ],
      "is_hurt_color":{},
      "on_fire_color":{}
    }
  }
}
